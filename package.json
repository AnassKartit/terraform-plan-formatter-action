{
  "name": "terraform-plan-formatter-action",
  "version": "1.0.0",
  "description": "Format Terraform plan output into a summary table",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["github", "action", "terraform"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  }
}
